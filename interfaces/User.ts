interface User { // not to be confused with User Interface, ha ha! Yes, I'm very bored.
  id: string;
  firstName: string;
  lastName: string;
  username: string;
  password: string;
  membershipStatus: string;
};

export default User;